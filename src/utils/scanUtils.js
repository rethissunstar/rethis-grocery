export const handleDetected = (code) => {
    console.log("Scanned UPC:", code);
  
    // TODO: Search your list, update localStorage, show a confirmation, etc.
    // Example:
    // if (code === '123456789012') {
    //   replaceItemInList('Cereal', 'Frosted Flakes');
    // }
  };
  